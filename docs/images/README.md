# Screenshot provenance

这些素材在 2026-08-24 通过本地真实前后端联调生成，浏览器视口为 1440 × 1000。

| File | Evidence |
| --- | --- |
| `01-home.png` | 首页、导航和响应式主视觉 |
| `02-gallery.png` | 搜索、筛选、排序、分页与缺图兜底 |
| `03-detail.png` | 详情字段、收藏/下载入口与相关推荐 |
| `04-assistant.png` | “热门图片”请求后的真实数据库工具结果 |
| `04-assistant.gif` | 15 秒：输入 → SSE → 后端图库工具 → 推荐结果 |
| `05-weather.png` | 天气面板、缓存状态与失败原因 |
| `06-admin.png` | 管理统计、导航和真实操作日志 |

生产 `uploads` 属于运行时数据且不进入 Git；本地数据库记录对应的生产图片文件不在工作区，所以图片卡片显示产品兜底状态。截图没有注入伪造性能、用户量或部署结果。

`07-deploy.png` 仅在真实服务器完成 `systemctl`、`curl` 和 `nginx -t` 验收后添加，截图必须遮盖 IP、Cookie、数据库地址和密钥。
